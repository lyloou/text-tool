# macOS 文本处理 App 实现计划

## 当前状态

当前项目已经从空目录搭建为一个基于 **Tauri + Rust + React** 的 macOS 文本处理 App 雏形，并已完成这些能力：

- 左侧 **原始内容工作台**
  - 原始内容输入
  - 查找替换
  - 输入区搜索高亮
  - 按行逆序
- 右侧 **结果工作台**
  - 忽略空行开关
  - 一次同时展示三种结果：
    - `"line1","line2"`
    - `'line1','line2'`
    - `line1,line2`
  - 每种结果单独复制
- Rust core 已有测试覆盖转换、替换、搜索、按行逆序

## 当前交互边界

### 原始内容工作台
只作用于左侧原始内容：

- 用户输入文本
- 查找替换
- 搜索关键词
- 输入区高亮命中
- 按行逆序

### 结果工作台
只作用于右侧结果区：

- 忽略空行
- 点击转换
- 同时展示三种格式结果
- 每种结果独立复制

## 当前工程结构

```text
text-tool/
├── frontend/
│   └── src/
│       ├── App.tsx
│       ├── App.css
│       ├── components/
│       │   ├── Toolbar.tsx
│       │   ├── InputEditor.tsx
│       │   └── ResultView.tsx
│       └── services/
│           └── tauriApi.ts
├── src-tauri/
│   └── src/
│       ├── main.rs
│       └── commands.rs
└── crates/
    └── text_core/
        └── src/
            ├── lib.rs
            ├── transform.rs
            ├── replace.rs
            ├── search.rs
            └── reorder.rs
```

## Rust core API

当前 `crates/text_core/` 已实现：

- `convert_lines(input, mode, ignore_empty_lines)`
- `replace_text(input, find, replace_with)`
- `search_matches(output, query, case_sensitive)`
- `reverse_lines(input)`

## 已完成验证

### 自动验证

- 前端构建通过
- Rust 单元测试通过
  - 三种转换模式
  - 空行保留 / 忽略
  - 普通替换
  - 搜索命中位置计算
  - 按行逆序

### 浏览器手动验证

已验证：

- 输入区搜索高亮只出现在原始内容区域
- 结果区不再高亮
- 替换后左侧原始内容与右侧三张结果卡片同步刷新
- 每张结果卡片可独立复制
- 忽略空行切换后，三张结果都正确更新
- 按行逆序后，左侧原始内容与右侧三张结果都同步逆序
- 控制台无错误

## 下一步要做

后续继续时，优先做这些能力：

1. **去重**
   - 对原始内容按行去重
   - 保持原始顺序
   - 作为原始内容工作台中的新操作

2. **排序**
   - 对原始内容按行排序
   - 先做升序
   - 如需要再补降序

3. **前后缀批量添加**
   - 对每一行批量添加前缀、后缀
   - 适合生成 SQL、代码片段、枚举值

4. **SQL `IN (...)` 片段生成**
   - 基于现有三种结果扩展更贴近 SQL 场景的结果卡片

5. **JSON 数组字符串生成**
   - 输出为 JSON 数组格式
   - 例如：`["line1", "line2"]`

6. **大小写转换**
   - 全部转小写
   - 全部转大写

## 打包验证待办

在继续功能扩展之外，还需要做一次完整的 macOS 打包验证：

### 1. Tauri 桌面端运行验证

- 启动 Tauri 开发模式
- 验证桌面窗口中的这些行为：
  - 原始内容输入
  - 搜索高亮
  - 查找替换
  - 按行逆序
  - 三种结果同步展示
  - 单独复制
  - 忽略空行切换

### 2. macOS `.app` 构建验证

- 运行 Tauri build
- 生成 `.app`
- 本机直接打开应用
- 做 smoke test

### 3. 剪贴板能力验证

- 在真实 Tauri 桌面端验证复制是否稳定
- 确认每张结果卡片复制内容准确

### 4. 窗口与样式验证

- 检查窗口初始尺寸是否合适
- 检查缩放或较小窗口下布局是否仍可用
- 检查输入区 overlay 高亮在桌面端是否对齐

### 5. 后续可选发布验证

如需外发，再继续做：

- `.dmg` 打包
- 代码签名
- notarization 公证
- Gatekeeper 校验

## 后续继续时的建议顺序

建议下一次继续开发时按这个顺序推进：

1. 先做 **Tauri 开发模式与 `.app` 打包验证**
2. 再做 **去重**
3. 再做 **排序**
4. 再做 **前后缀批量添加**
5. 最后补 **SQL IN / JSON 数组** 这类结果模板
